#version 450

// Placeholder compute shader for the staged Vulkan path.
// The CPU reference simulation owns correctness until the Vulkan backend lands.
layout(local_size_x = 8, local_size_y = 8, local_size_z = 8) in;

void main()
{
}
